// import * as icons from "../assets/icons";

// type IconName = keyof typeof icons;

// type Props = {
//   name: IconName;
//   fill: string;
// };

// export const SvgIcon: React.FC<Props> = ({ name, fill }) => {
//   const Icon = icons[name];
//   return <Icon fill={fill} />;
// };

const random = () => undefined;

export default random;
